#include <iostream>

int main()
{
//Step 1
/*	std::cout"Hello world" <<std::endl;	//Raises error no "<<" for the cout command. //Syntax Error
	//Correct would be 
	std::cout<< "Hello world" <<std::endl;
	
	/*for(int i= 5; i>=0; i-- )
	{
		std::cout<< "10 divided by " <<i <<" is " <<(10/i) <<std::endl;
		//Code is syntaxically correct, but runs into a logic error once it reaches the final iteration.
			//This is because it will attempt to divide by 0, which is a mathmatical impossibility.
			//Code is also called a runtime error
	}*/
	
	/*for(int i=0; i==0; i+=0 )
	{
		//Raises an error as the iteration will never stop, the program seemingly hangs.
			//It does not stop since the condition in the for loop is not being made invalid, and the iterative element is not 
			//Actually chaning the value for this to be the case.
	}*/
//Step 2
	/*float  theFloat= 1.0f;
	
	std::cout<<"theFloat has a value of " <<theFloat <<std::endl;
	std::cout<< "theFloat has a memaddr of " <<&theFloat <<std::endl;
	
	//Define pointer
	float* theFloat_Pointer= &theFloat;	//Decalre and define pointer
	std::cout<<"theFloat_pointer has a value of " <<*theFloat_Pointer <<std::endl;	//Deref pointer
	//change the value 
	(*theFloat_Pointer )+= 4.0f;
	std::cout<<"theFloat has a memaddr of " <<*theFloat_Pointer <<std::endl;	//find value which is being pointed to*/
	
//Step 3
	 
}
